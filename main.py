from drawer import draw_unit_circle_interactive
from recurrences import *

if __name__ == "__main__":
    draw_unit_circle_interactive(
        theta,
        phi_p=phi_p,
        delta_p=delta_p,
        phi_m=phi_m,
        delta_m=delta_m,
    )
